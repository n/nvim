-- https://docs.astral.sh/ty/reference/editor-settings/
return {
  settings = {
    ty = {
      -- Disable language services to prevent conflicts with basedpyright.
      disableLanguageServices = true,
    },
  },
}
